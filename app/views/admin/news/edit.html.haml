.container
  .row
    .col-md-6
      .panel.panel-success
        .panel-heading
          Редактируем сообщение
        .panel-body
          = simple_form_for @new, url: {action: 'update'} do |f| 
            = f.input :title, label: 'Заголовок' 
            = f.input :description, as: :text
            = f.input :date, label: 'Дата'
            = f.button :submit, 'Обновить'
